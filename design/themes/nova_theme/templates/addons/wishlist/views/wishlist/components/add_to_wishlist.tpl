{include file="design/themes/responsive/templates/addons/wishlist/views/wishlist/components/add_to_wishlist.tpl"
    wishlist_button_type=$wishlist_button_type
    wishlist_but_id=$wishlist_but_id
    wishlist_but_name=$wishlist_but_name
    wishlist_but_title=$wishlist_but_title
    wishlist_but_role=$wishlist_but_role
    wishlist_but_onclick=$wishlist_but_onclick
    wishlist_but_href=$wishlist_but_href
    wishlist_but_icon=$wishlist_but_icon
    wishlist_but_text=$wishlist_but_text
    wishlist_but_meta=$wishlist_but_meta
    quick_view=$quick_view
    runtime=$runtime
    block=$block
    product=$product
    enable_grouping_buttons_product=$enable_grouping_buttons_product|default:false
}