<button title="{$alt}" class="ty-btn-go {$but_meta}" type="submit">{if $but_text}{$but_text}{else}{include_ext file="common/icon.tpl" class="ty-icon-right-dir ty-btn-go__icon `$but_icon_meta`"}{/if}</button>
<input type="hidden" name="dispatch" value="{$but_name}" />
